import 'package:flutter/material.dart';

class ForgotPasswordSent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Reset password sent. Please check your email'),
    );
  }
}
